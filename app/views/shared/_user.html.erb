<li class="user">
  <% if user.photo.present? %>
    <%= image_tag user.photo %>
  <% end %>
  <div class="blue-box">
    <h2><%= link_to user.name, user_path(user) %></h2>
    <p>Number of posts: <%= user.posts.any? ? user.postCounter : 0 %></p>
  </div>
</li>
